class Sprite {

  constructor({position, height, width}) {
    this.position = position;
    this.height = height;
    this.width = width;
  }

  draw() {
    
  }

  update() {
    this.draw();
  }

}